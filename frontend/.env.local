# MongoDB connection URI (replace with your actual connection string)
MONGO_URI=mongodb://localhost:27017/myEcommerceDB

# JWT secret key (use a strong, random string here)
JWT_SECRET=yourSuperSecretJWTKey12345!

# Server port (optional, default is 5000 if not specified)
PORT=5000

# Environment mode (enables detailed error messages for debugging)
NODE_ENV=development